{
  "name": "processfactory-repos",
  "private": true,
  "scripts": {
    "init": "lerna init",
    "setup": "lerna bootstrap",
    "release": "lerna publish --force-publish",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.0",
    "shelljs": "^0.8.4"
  }
}
